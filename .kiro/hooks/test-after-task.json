{
  "version": "v1",
  "hooks": [
    {
      "name": "Run Tests After Task Completion",
      "trigger": "PostTaskExec",
      "description": "Runs pytest after a spec task is marked complete to validate implementation meets acceptance criteria",
      "action": {
        "type": "command",
        "command": "cd /Users/admin/Documents/Proyectos/Runbook_Guardian && python -m pytest tests/ -x -q --tb=short 2>/dev/null || echo \"Tests failed or not yet configured\"",
        "timeout": 60
      }
    }
  ]
}