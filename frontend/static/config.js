// Configuración del frontend — reemplazar después del deploy
// Este archivo se genera automáticamente con la URL de la API Gateway
window.API_BASE_URL = "";  // Se autoconfigura: vacío usa la misma URL base
